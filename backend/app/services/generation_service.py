"""Orchestrates AI architecture generation.

Functionality has been removed from this build. The class is kept as a
structural stub so the dependency graph still resolves.
"""

from __future__ import annotations

_DISABLED = "Architecture generation has been removed from this build."


class GenerationService:
    def __init__(self, *args, **kwargs) -> None:
        pass

    def generate(self, project):
        raise NotImplementedError(_DISABLED)
