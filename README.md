# Archsari

**Architecture Before Code** — plan application architecture before development.

Archsari is a wizard-driven tool that turns project requirements into a structured architecture document: components, cloud options (AWS, GCP, Azure), cost estimates, risks, recommendations, and diagrams. It stays technology-agnostic (no language or framework prescriptions). Architecture content is generated by AI from your inputs; cost estimates and cloud mapping use fixed rules.
